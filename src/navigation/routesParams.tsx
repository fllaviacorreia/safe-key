// Define o tipo de objeto que contém os parâmetros das rotas
export type RoutesParams = {
    
    // A rota "Login" não exige nenhum parâmetro, então seu valor é definido como "undefined"
    Login: undefined;
    
    // A rota "Register" também não exige parâmetros, por isso é "undefined"
    Register: undefined;
    
    // A rota "ResetPassword" não requer parâmetros, então também é "undefined"
    ResetPassword: undefined;
    
    // A rota "Home" também não exige parâmetros e, portanto, é "undefined"
    Home: undefined;

    // A rota "Profile" não exige parâmetros e, portanto, é "undefined"
    Profile: undefined;
};
